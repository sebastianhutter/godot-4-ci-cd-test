name: Run tests on pull request (on mac runners for reference!)
on:
  workflow_dispatch:
permissions:
    checks: write
jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - id: download
        name: download godot
        uses: ./.github/actions/mac/download-godot
        with:
          version: ${{ vars.GODOT_VERSION }}
      - name: run tests
        uses: ./.github/actions/mac/run-gdunit4
        with:
          binary: ${{ steps.download.outputs.godot-bin }}
      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v4
        if: success() || failure() # always run even if the previous step fails
        with:
          report_paths: '**/reports/report_1/**/results.xml'
          require_tests: true